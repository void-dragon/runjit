# do some lambda
call = (a) => { print(a) }
# call("yes")
call(1)

# go for the dict
x = { some: "content" }

# arrays, everyone should have one
arr = [1, 2, "3"]

# simple expression
myvar = 42 + 6
